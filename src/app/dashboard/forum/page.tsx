import ForumPage from "@/app/forum/page";

// For this project, the dashboard forum view is the same as the public one.
// In a real app, this could be customized to show "My Threads" or "Subscribed Topics".
export default ForumPage;
